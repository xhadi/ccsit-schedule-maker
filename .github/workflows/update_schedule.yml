name: Update Course Schedule

on:
  schedule:
    # Runs every 2 hours (adjust as needed)
    - cron: "0 */2 * * *"
  workflow_dispatch: # Allows manual trigger button

permissions:
  contents: write

jobs:
  scrape-and-update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.ACTION_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"

      - name: Install dependencies
        run: |
          pip install -r scraper/requirements.txt
<<<<<<< HEAD
          playwright install chromium --with-deps
=======
>>>>>>> b2b9606eb5aed58d5fd0fe66172925adc0aeb8aa

      - name: Run Scraper
        run: python scraper/scrap.py

      - name: Commit and Push if Changed
        run: |
          git config user.name "KFU Scheduler Bot"
          git config user.email "actions@github.com"
          git add public/*.csv
          # The command below checks if there are changes before committing
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-update course schedule" && git push)
